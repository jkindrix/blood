// Simple array match test

fn test_prefix_only(arr: [i32; 3]) -> i32 {
    match arr {
        [first, _, _] => first,
        _ => 0,
    }
}

fn main() -> i32 {
    let arr = [10, 20, 30];
    test_prefix_only(arr)
}

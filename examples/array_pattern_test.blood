// Test array pattern matching

fn test_array(arr: [i32; 3]) -> i32 {
    match arr {
        [a, b, c] => a + b + c,
        _ => 0,
    }
}

fn main() -> i32 {
    let arr = [10, 20, 30];
    test_array(arr)
}

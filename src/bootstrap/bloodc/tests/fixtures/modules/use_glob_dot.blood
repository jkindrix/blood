// Canary test: glob import with dot-separated path (Phase A, D1).
// Verifies `use module.*` syntax works identically to `use module::*`.

mod helper;

// Glob import using dot before *
use helper.*;

pub fn main() -> i32 {
    let p: Point = Point::new(1, 2);
    let k: Kind = Kind::A;
    let v: i32 = get_value();
    let m: i32 = MAX_VALUE;
    let n: MyInt = 10;

    p.x + v + m + n
}

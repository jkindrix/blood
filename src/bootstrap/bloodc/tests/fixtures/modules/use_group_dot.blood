// Canary test: grouped import with dot-separated path (Phase A, D1).
// Verifies `use module.{A, B}` syntax works identically to `use module::{A, B}`.

mod helper;

// Group import using dot before {}
use helper.{Point, Kind, get_value, MAX_VALUE};

pub fn main() -> i32 {
    let p: Point = Point::new(1, 2);
    let k: Kind = Kind::A;
    let v: i32 = get_value();
    let m: i32 = MAX_VALUE;

    p.x + v + m
}

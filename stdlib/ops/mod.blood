// Blood Standard Library - Operator Traits
//
// Entry point for `use std.ops.*;`. Currently exposes:
//   - deref::Deref — overloads the unary `*` operator.
//
// Future modules (uncomment as implementations land):
// pub mod add;       // Add, AddAssign
// pub mod sub;       // Sub, SubAssign
// pub mod mul;       // Mul, MulAssign
// pub mod div;       // Div, DivAssign
// pub mod index;     // Index, IndexMut
// pub mod range;     // Range types

pub mod deref;

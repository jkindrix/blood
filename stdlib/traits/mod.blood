// Blood Standard Library - Traits
//
// Entry point for `use std.traits.*;` imports. Exposes only modules
// that compile standalone in the current language. `marker.blood`
// (supertrait bounds, const generics, PhantomData) is intentionally
// NOT exposed — see its top-of-file banner.

pub mod hash;
pub mod clone;
pub mod default;
pub mod debug;

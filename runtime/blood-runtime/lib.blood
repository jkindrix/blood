// Blood Runtime Library — entry point
//
// Compiled standalone to produce libblood_runtime_blood.a
// Module ordering follows dependency chain:
//   libc (no deps) -> print/panic -> alloc -> region -> vec -> string -> ...

mod libc;
mod print;
mod rt_panic;
mod alloc;
mod rt_region;
mod rt_string;
mod rt_vec;
mod rt_box;
mod rt_io;
mod rt_cli;
mod rt_evidence;
mod rt_effect;
mod rt_continuation;

// Dummy main — required by compiler, never called (this is a library)
fn main() -> i32 { 0 }

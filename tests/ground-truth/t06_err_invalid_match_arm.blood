// COMPILE_FAIL: invalid match arm syntax
// EXPECT: [E0100] expected literal, identifier, `(`, `[`, `{`, `if`, or `match`, found `,`
// Test: match arm with missing body should fail (E0115)
fn main() -> i32 {
    match 42 {
        1 =>,
        _ => 0,
    }
}

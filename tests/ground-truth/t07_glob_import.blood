// Test: glob imports (use path.*)
// EXPECT: 42
mod t07_glob_import_lib;
use t07_glob_import_lib.*;

fn main() -> i32 {
    // Use struct, functions, and const imported via glob
    let p: Point = make_point(10, 32);
    let s: i32 = sum_point(&p);
    println_int(s);
    if s != MAGIC { return 1; }
    0
}

// COMPILE_FAIL: E0200
// EXPECT: [E0100] expected identifier, literal, `_`, `(`, `[`, or `..`, found reserved keyword `defer`
// Test: reserved keywords cannot be used as identifiers

fn main() -> i32 {
    let defer: i32 = 0;
    0
}

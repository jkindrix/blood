// COMPILE_FAIL: no main function (helper module for t07_glob_import)

pub struct Point {
    pub x: i32,
    pub y: i32,
}

pub fn make_point(x: i32, y: i32) -> Point {
    Point { x, y }
}

pub fn sum_point(p: &Point) -> i32 {
    p.x + p.y
}

pub const MAGIC: i32 = 42;

// Private item — should NOT be imported by glob
fn private_helper() -> i32 {
    0
}

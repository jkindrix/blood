// Test: eprint and eprintln builtins (stderr output)
// Verify that eprintln_str writes to stderr without crashing,
// then confirm via stdout output that execution continued normally.
// EXPECT: 42
fn main() -> i32 {
    eprintln_str("error message");
    println_int(42);
    0
}

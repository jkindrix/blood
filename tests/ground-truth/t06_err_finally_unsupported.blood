// COMPILE_FAIL: E0305
// EXPECT: [E0305] invalid effect handler: finally clauses are not yet supported
// Test: finally clause emits diagnostic (not silently dropped)
effect Logger {
    op log(msg: i32) -> ();
}

deep handler NullLogger for Logger {
    return(x) { x }

    finally {
        0
    }

    op log(msg) {
        resume(())
    }
}

fn main() -> i32 {
    0
}

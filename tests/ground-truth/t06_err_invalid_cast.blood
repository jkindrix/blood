// COMPILE_FAIL: invalid cast
// EXPECT: [E0239] casting `Foo` as `i32` is invalid
// Test: casting between incompatible types should fail (E0239)
struct Foo {
    x: i32,
}

fn main() -> i32 {
    let f = Foo { x: 42 };
    let n = f as i32;
    n
}

// EXPECT: 1
// Tests multi-capture macro: two captures separated by comma.
macro check_eq {
    ($a:expr, $b:expr) => {
        if $a == $b { 1 } else { 0 }
    };
}

fn main() {
    let result = check_eq!(21 + 21, 42);
    println(int_to_string(result));
}

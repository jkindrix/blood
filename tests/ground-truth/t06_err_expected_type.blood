// COMPILE_FAIL: expected type after colon
// EXPECT: [E0100] expected type name, `// COMPILE_FAIL: expected type after colon`, `*`, `[`, `(`, `fn`, `forall`, or `!`, found `=`
// Test: missing type annotation after colon should fail (E0109)
fn main() -> i32 {
    let x: = 42;
    0
}

// COMPILE_FAIL: assignment to immutable variable
// EXPECT: [E0238] cannot assign twice to immutable variable `x`
// Test: assigning to an immutable let binding should fail
fn main() -> i32 {
    let x: i32 = 10;
    x = 20;
    0
}

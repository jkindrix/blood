// COMPILE_FAIL: no main function (helper module for t07_pub_use_glob)

mod inner {
    pub fn add(a: i32, b: i32) -> i32 {
        a + b
    }

    pub fn mul(a: i32, b: i32) -> i32 {
        a * b
    }

    pub const BASE: i32 = 10;
}

// Re-export everything from inner via glob
pub use inner::*;

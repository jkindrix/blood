// COMPILE_FAIL: cast requires @unsafe
// Test that Ptr↔usize casts require @unsafe context.

fn main() -> i32 {
    let x: i32 = 42;
    let ptr: *const i32 = &x as *const i32;

    // This should fail — Ptr→usize outside @unsafe
    let addr: usize = ptr as usize;

    0
}

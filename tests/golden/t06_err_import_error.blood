// COMPILE_FAIL: import resolution error
// EXPECT: [E0100] expected `*`, `{`, or identifier, found keyword `module`
// Test: importing a nonexistent path should fail (E0704)
use nonexistent.module.Item;

fn main() -> i32 {
    0
}

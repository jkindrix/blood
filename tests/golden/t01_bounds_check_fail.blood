// Test: out-of-bounds Vec access produces a runtime panic.
// EXPECT_EXIT: nonzero

fn main() -> i32 {
    let mut v: Vec<i32> = Vec.new();
    v.push(10);
    v.push(20);
    // Index 5 is out of bounds (length is 2)
    let val: i32 = v[5];
    println_int(val);
    0
}

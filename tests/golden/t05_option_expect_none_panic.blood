// Test: Option<T>.expect(msg) on None aborts with the user-supplied msg.
// Verifies the panic path of the new rt_option_expect runtime function.
// EXPECT_EXIT: nonzero

fn main() -> i32 {
    let n: Option<i32> = None;
    let v: i32 = n.expect("missing value here");
    println_int(v);
    0
}

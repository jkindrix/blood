// COMPILE_FAIL: main function must have no parameters (E0219)
// EXPECT: [E0219] `main` function has wrong signature
// Test: main with wrong parameter signature should fail
fn main(x: i32) -> i32 {
    x
}

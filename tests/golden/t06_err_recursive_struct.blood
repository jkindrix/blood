// COMPILE_FAIL: recursive struct with infinite size
// EXPECT: [E0205] recursive type `S` has infinite size
// Test: directly recursive struct should fail
struct S {
    value: i32,
    next: S,
}

fn main() -> i32 {
    0
}

// COMPILE_FAIL: invalid pattern
// EXPECT: [E0100] expected identifier, literal, `_`, `(`, `[`, or `..`, found keyword `if`
// Test: keyword in pattern position should fail (E0106)
fn main() -> i32 {
    let if = 3;
    0
}

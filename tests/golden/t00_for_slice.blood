// Test: for-in loop over a byte slice (&[u8])
// EXPECT: 532
fn main() -> i32 {
    let s = "hello";
    let bytes = s.as_bytes();
    let mut sum: i32 = 0;
    for b in bytes {
        sum = sum + (*b as i32);
    }
    println_int(sum);
    0
}

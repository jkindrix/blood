// Test: block-like expressions (if, match) followed by a new expression
// on the next line are treated as separate statements.
// EXPECT: 0

fn main() -> i32 {
    let x: i32 = if true { 10 } else { 20 }
    let y: i32 = x + 5
    println_int(y - 15);
    y - 15
}

// Test: where clause on generic function
// EXPECT: 0
trait Printable {
    fn to_int(self) -> i32;
}

fn print_it<T>(x: T) -> i32 where T: Printable {
    x.to_int()
}

fn main() -> i32 {
    println_int(0);
    0
}

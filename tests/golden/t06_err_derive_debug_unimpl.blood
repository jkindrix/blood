// COMPILE_FAIL: `#[derive(Debug)]` is a known kind but expansion isn't
// yet implemented
// EXPECT: not yet implemented
// EXPECT: Debug
//
// C3 Phase B (S69): `expand_request` now emits E0901 when a known derive
// kind cannot be expanded — either because the kind itself isn't yet
// implemented (Debug as of S70) or because the target's shape is
// unsupported (tuple/unit struct, record-variant enum, oversized tuple
// variants). Clone, Default, Eq, Hash, and PartialEq ship in S67/S68/S69/S70;
// Debug remains unimplemented and emits this diagnostic.
#[derive(Debug)]
struct Point {
    x: i32,
    y: i32,
}

fn main() -> i32 {
    let _p = Point { x: 1, y: 2 };
    0
}

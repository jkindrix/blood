// Test: division by zero produces a runtime panic, not SIGFPE or UB.
// EXPECT_EXIT: nonzero

fn main() -> i32 {
    let x: i32 = 10;
    let y: i32 = 0;
    let z: i32 = x / y;
    println_int(z);
    0
}

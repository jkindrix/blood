// Test: in-bounds Vec access works normally.
// EXPECT: 42

fn main() -> i32 {
    let mut v: Vec<i32> = Vec.new();
    v.push(10);
    v.push(42);
    v.push(99);
    let val: i32 = v[1];
    println_int(val);
    if val != 42 { return 1; }
    0
}

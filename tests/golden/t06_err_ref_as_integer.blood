// COMPILE_FAIL: E0239
// EXPECT: [E0239] casting
// Test: &T as integer is rejected — generational refs lose safety info (TYP-05)

struct Foo {
    pub x: i32,
}

fn main() -> i32 {
    let f = Foo { x: 42 };
    let r: &Foo = &f;
    let addr: u64 = r as u64;
    0
}

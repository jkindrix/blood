// Test: generic function called at two different types in same scope
// Both monomorphizations should produce correct results
fn double<T>(x: T) -> T {
    x
}

fn main() -> i32 {
    let a: i32 = double(21);
    let b: bool = double(true);
    if b {
        a - 21
    } else {
        1
    }
}

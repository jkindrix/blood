// COMPILE_FAIL: binary operator type checking for non-numeric types
// EXPECT: [E0216] cannot apply `+` to `bool` and `bool`
// Test: adding two booleans should fail
fn main() -> i32 {
    let x: bool = true + false;
    0
}

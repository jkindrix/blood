// Test: affine value used in one branch but not the other should be valid
// Affine = at-most-once. Using in one branch and not the other is fine.
// EXPECT: 0
fn main() -> i32 {
    let x: affine i32 = 42;
    let cond: bool = true;
    if cond {
        let y: affine i32 = x;
    } else {
        // x not used — valid for affine (at-most-once)
    }
    println_int(0);
    0
}

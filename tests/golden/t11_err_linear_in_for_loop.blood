// COMPILE_FAIL: E0804
// EXPECT: [E0804] linear value `x: linear i32` consumed inside loop
// Test: linear value consumed in for loop body should fail
// Same rule as while loops — linear defined outside cannot be consumed inside.
fn main() -> i32 {
    let x: linear i32 = 42;
    for i in 0..3 {
        let y: linear i32 = x;
    }
    0
}

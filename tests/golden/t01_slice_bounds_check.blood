// Test: slice indexing with valid index succeeds, bounds check passes
// EXPECT: 104
fn main() -> i32 {
    let s = "hello";
    let bytes = s.as_bytes();
    let x: u8 = bytes[0];
    println_int(x as i32);
    0
}

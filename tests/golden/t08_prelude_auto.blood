// EXPECT: 42
// Tests that stdlib prelude items are auto-imported when --stdlib-path is set.
// Prelude functions are available without explicit `use std.prelude.*;`.
mod std;

fn main() {
    let a = min(10, 20);
    let b = max(30, 42);
    let c = abs_i32(0 - 5);
    let d = clamp(100, 0, 50);
    // a=10, b=42, c=5, d=50
    // Result: b (42)
    println(int_to_string(b));
}

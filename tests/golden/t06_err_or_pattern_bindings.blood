// COMPILE_FAIL: E0403
// EXPECT: or-pattern alternatives bind different variables; missing: x
// Test: or-pattern alternatives must bind the same variables
enum Opt {
    Some(i32),
    None,
}

fn main() -> i32 {
    let val = Opt.Some(42);
    match val {
        Opt.Some(x) | Opt.None => { 0 }
    }
}

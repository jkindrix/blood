// COMPILE_FAIL: without a body
// EXPECT: [E0228] unsupported feature: function `foo` declared without a body; only trait methods and extern functions may omit the body
// Test: function declaration without body outside trait should fail
fn foo() -> i32;

fn main() -> i32 {
    0
}

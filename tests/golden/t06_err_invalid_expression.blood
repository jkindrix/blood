// COMPILE_FAIL: invalid expression
// EXPECT: [E0100] expected literal, identifier, `(`, `[`, `{`, `if`, or `match`, found keyword `fn`
// Test: keyword where expression expected should fail (E0107)
fn main() -> i32 {
    let x: i32 = fn;
    0
}

// COMPILE_FAIL: cannot cast
// Test: bridge type alias — lowered but typeck doesn't follow aliases yet.

bridge "C" Types {
    type SizeT = u64;
}

fn main() -> i32 {
    let s: Types.SizeT = 42;
    println_int(s as i64);
    0
}

// Test: continuation tokens allow expressions to span multiple lines.
// A binary operator at the start of a new line continues the expression.

fn main() -> i32 {
    let x: i32 = 10
        + 20
        + 30
    x - 60
}

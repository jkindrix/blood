// COMPILE_FAIL: duplicate variant name in enum
// EXPECT: duplicate variant name `Red` in enum
// Test: enum with duplicate variant names should fail
enum Color {
    Red,
    Blue,
    Red,
}

fn main() -> i32 {
    0
}

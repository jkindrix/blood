// COMPILE_FAIL: expected pattern in match arm
// EXPECT: [E0100] expected identifier, literal, `_`, `(`, `[`, or `..`, found `=>`
// Test: match arm with no pattern before arrow should fail (E0111)
fn main() -> i32 {
    match 42 {
        => 0,
    }
}

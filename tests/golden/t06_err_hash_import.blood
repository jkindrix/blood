// COMPILE_FAIL: hash import with unknown prefix
// EXPECT: unresolved hash import
// Test: use hash("prefix") with no matching codebase entry should fail
use hash("deadbeef0000");

fn main() -> i32 {
    0
}

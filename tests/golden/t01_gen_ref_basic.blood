// Test: basic generational reference creation and dereference
// EXPECT: 42
// EXPECT: 100

fn main() {
    let x: i32 = 42;
    let r: &i32 = &x;
    let v1: i32 = *r;
    println_int(v1);

    let y: i32 = 100;
    let s: &i32 = &y;
    let v2: i32 = *s;
    println_int(v2);
}

// Test: generic method that returns the generic type
// EXPECT: 42
// EXPECT: true

struct Identity {}

impl Identity {
    fn id<T>(self, x: T) -> T {
        x
    }
}

fn main() -> i32 {
    let i: Identity = Identity {};
    println_int(i.id(42));
    println_bool(i.id(true));
    0
}

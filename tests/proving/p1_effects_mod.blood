// Module for P1 effectful: demonstrates effects across module boundaries.
// The Emit<T> effect is defined in the main file; this module performs it.

fn emit_from_module(val: i32) / {Emit<i32>} {
    perform Emit.emit(val);
}

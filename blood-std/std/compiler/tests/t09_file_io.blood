// Test: basic file I/O operations
// EXPECT: hello from file
fn main() -> i32 {
    let path: &str = "/tmp/blood_test_file_io.txt";
    file_write_string(path, "hello from file");
    let content: &str = file_read_to_string(path);
    println_str(content);
    file_delete(path);
    0
}

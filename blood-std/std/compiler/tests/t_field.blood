struct Pair {
    first: i32,
    second: i32,
}

fn main() -> i32 {
    let p = Pair { first: 10, second: 20 };
    let f = p.first;
    let s = p.second;
    println_i32(f);
    println_i32(s);
    0
}

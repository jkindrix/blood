// Test: Vec creation, push, len, and indexing
// EXPECT: 3
// EXPECT: 10
// EXPECT: 20
// EXPECT: 30
fn main() -> i32 {
    let mut v: Vec<i32> = Vec::new();
    v.push(10);
    v.push(20);
    v.push(30);
    println_int(v.len() as i32);
    println_int(v[0]);
    println_int(v[1]);
    println_int(v[2]);
    0
}

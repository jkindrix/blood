// Test: if/else, while loops
// EXPECT: positive
// EXPECT: 55
fn main() -> i32 {
    let x: i32 = 5;
    if x > 0 {
        println("positive");
    } else {
        println("non-positive");
    }

    // Sum 1..10
    let mut sum: i32 = 0;
    let mut i: i32 = 1;
    while i <= 10 {
        sum = sum + i;
        i = i + 1;
    }
    println_int(sum);
    0
}

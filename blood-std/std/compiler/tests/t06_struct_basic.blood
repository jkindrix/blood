// Test: struct creation and field access
// EXPECT: 10
// EXPECT: 20
pub struct Point {
    pub x: i32,
    pub y: i32,
}

fn main() -> i32 {
    let p = Point { x: 10, y: 20 };
    println_int(p.x);
    println_int(p.y);
    0
}

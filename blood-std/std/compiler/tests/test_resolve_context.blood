pub struct Entry {
    pub parent: Option<u32>,
}

pub fn test(entry: &Entry) -> Option<u32> {
    match &entry.parent {
        &Option::Some(ref p) => {
            Option::Some(*p)
        }
        &Option::None => Option::None,
    }
}

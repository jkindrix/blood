struct TwoVecs {
    first: Vec<i32>,
    second: Vec<i32>,
}

impl TwoVecs {
    fn fill(self: &mut TwoVecs) {
        self.first.push(1);
        self.first.push(2);
        self.first.push(3);
        self.first.push(4);
    }
}

fn main() -> i32 {
    let mut tv = TwoVecs { first: Vec::new(), second: Vec::new() };
    tv.fill();
    let len = tv.first.len();
    println_i64(len as i64);
    0
}

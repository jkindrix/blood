//! Span re-exports for backward compatibility.
//!
//! This module re-exports span types from `std.compiler.diagnostics.span`
//! to maintain compatibility with code that imports from `std.compiler.span`.

pub use std.compiler.diagnostics.span::{
    Span,
    Spanned,
    FileId,
    Position,
    LineIndex,
};

# Corpus programs known to fail, with the reason. run_corpus.sh fails CI only
# on a failure NOT listed here -- a regression. Listed failures are still work:
# tools/health.sh reports them, and CLAUDE.md rule 2 says fix them next.
#
# Remove an entry the moment its program passes; run_corpus.sh says when.
#
# project    reason
brainfuck    phantom builtin print_char -- see tools/builtin-parity.sh
sortbench    phantom builtin print_u64  -- see tools/builtin-parity.sh
identity     content hashes of any definition that references another (incl. recursive
             self-calls) depend on compiler-internal position, not content. Hardcoded
             prefixes a13d/40f0/2ceb were computed by a March build; today's compiler
             hashes the same unchanged source to b01e/e454/83d4. Fixing it needs: the
             canonicalizer fix (CLAUDE.md queue), then a setup step here that runs
             `build --store-codebase mathlib.blood` first, then updated prefixes.

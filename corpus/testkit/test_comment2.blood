// double_check! -> assert_eq!
macro assert_eq {
    ($a:expr, $b:expr) => {
        if $a == $b { 1 } else { 0 }
    };
}
macro double_check {
    ($x:expr, $expected:expr) => {
        assert_eq!($x + $x, $expected)
    };
}
fn main() -> i32 {
    0
}

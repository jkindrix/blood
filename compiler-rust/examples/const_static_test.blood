// Test const and static items

const PI: i32 = 3;
const MAX_SIZE: i32 = 100;
const NEGATIVE: i32 = -42;

static mut COUNTER: i32 = 0;
static THRESHOLD: i32 = 50;

fn main() -> i32 {
    PI + MAX_SIZE
}
